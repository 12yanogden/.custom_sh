#include <string>

template <class T>

class Array {
private:
    T data = [];

public:
    Array(const string *str, const char *delimiter);
};

Array::Array(const string *str, const char *delimiter) {

}